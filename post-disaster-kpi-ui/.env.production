# Production environment variables for Docker deployment

# Client-side: empty means use same origin (via nginx proxy)
NEXT_PUBLIC_API_URL=

# Server-side: Next.js API routes forward to backend at localhost:8083
BACKEND_API_URL=http://localhost:8083
